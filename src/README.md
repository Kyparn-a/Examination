# Playwright och BDD
Installera pytest-playwright och behave genom att skriva i terminalen:
```commandline
pip install pytest-playwright
playwright install
pip install behave
```

Starta testerna genom att skriva i terminalen:

```commandline
cd src/
behave
```

Adress till appen: ´https://tap-vt25-testverktyg.github.io/exam--reading-list/´
___